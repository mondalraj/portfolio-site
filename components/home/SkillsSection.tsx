import { Box } from "@mantine/core";

const SkillsSection = () => {
  return (
    <Box>
      Next, Typescript, Nodejs, MongoDB, Postgresql, mongodb, prisma, Tailwind
      CSS, Material UI, Mantine UI, Nestjs, Framer Motion
    </Box>
  );
};

export default SkillsSection;
